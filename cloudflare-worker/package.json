{
  "name": "alfred-jq-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for jq-like JSON processing",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test.js"
  },
  "devDependencies": {
    "wrangler": "3.78.12"
  }
}
